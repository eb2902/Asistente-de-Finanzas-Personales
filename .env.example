# Variables de entorno para el proyecto Asistente de Finanzas Personales

# Token de npm (requerido para instalar dependencias)
# Obtén tu token desde https://www.npmjs.com/settings/your-username/tokens
# Copia el token y guárdalo en un archivo .env (no subas este archivo al repositorio)
NPM_TOKEN=your_npm_token_here

# Variables para el backend
# Puerto donde correrá el servidor backend
PORT=3001

# Cadena de conexión a la base de datos PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/finanzas_db

# Clave secreta para JWT (genera una cadena aleatoria segura)
JWT_SECRET=tu_clave_secreta_jwt_aqui

# URL de OpenAI API (si se usa)
OPENAI_API_KEY=tu_clave_api_openai_aqui

# Variables para el frontend
# URL del backend API
NEXT_PUBLIC_API_URL=http://localhost:3001

# Entorno (development, production)
NODE_ENV=development